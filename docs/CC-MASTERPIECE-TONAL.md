# CC-MASTERPIECE-TONAL — Paintings Extract by Tone, Not Line
[Received from Eric 2026-08-04, verbatim in chat; D1-D4 SIGNED. This stub records
the operative law; full text in the session transcript.]

Sfumato is the deliberate elimination of lines — the line-trace failure was a
medium mismatch, not an instruction failure. TONAL pipeline: posterize 4-6
luminance bands (D1: per-piece, in-tool) -> band boundaries as contour paths ->
band interiors filled with structure-tensor flow paths -> words-as-shading,
monotonic (darker band = denser/heavier, LINTED). Vision landmarks seed
protected feature paths (suggestion-only, Eric approves); requiredFeatures per
subject blocks export when missing (mona: leftEye rightEye leftBrow rightBrow
nose mouth chin hairMass leftHand rightHand frame highlightBand). The glow =
lightest band, tagged, capped, own palette entry. Background = expert-tier
layer only when included (D2: mona INCLUDES it). extractionClass LINE|TONAL is
registry data; LINE pipeline byte-untouched; LINE goldens hash-pinned.
T0 tool-side ONLY (D3 resolves SCAN-STACK D1 for tool scope; app stays blocked
on P1/P2). D4: ONE approval session = tonal golden + PICTURE-COLOR side-by-side;
her verdict gates BOTH masterpiece lanes. Bar: striking typographic portrait,
not photorealism. No ML path generation; no runtime extraction; references
never ship.
