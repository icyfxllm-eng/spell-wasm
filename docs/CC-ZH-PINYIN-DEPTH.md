# CC-ZH-PINYIN-DEPTH v1

**Status:** RECEIVED 2026-08-29, NOT STARTED. Held at Gate 0.

**Gate 0 (the file's own words):** *"CC-ZH-PINYIN-DISPLAY complete and Eric's
device pass recorded. DEPTH does not start before this."* And: *"Sequenced
behind CC-ZH-PINYIN-DISPLAY (shares the matcher/canonical-form surface). Do not
run these concurrently."*

DISPLAY's F1 merged 2026-08-29. Still outstanding there: F3 font coverage, F4
span/grapheme, F6 verdict fidelity, and the device pass — which is what closes
that file, not the suite.

**D5 and D6 are unsigned and block F7 regardless.**

The full text of this spec is Eric's, held verbatim in the conversation of
2026-08-29. It is not restated here because nothing in it has been executed and
a half-copied spec is worse than a pointer to the real one.

One note recorded now because it will matter at F0.3: DISPLAY F6 (verdict
fidelity) is DEPTH's named input source. DEPTH F0 says to stop and ask if
`TONE_MISS` is unimplemented, and that question is currently unanswered — F6
has not been investigated. It should be answered as part of finishing DISPLAY,
not discovered at DEPTH's F0.
