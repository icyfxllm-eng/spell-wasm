{
 "_comment": "Masters known to fail the density floor, held so the gate stays green while they are re-authored. This list SHRINKS ONLY — the check fails if an entry starts passing (remove it) or names a picture not in the bank. Adding an entry is a deliberate act that should be argued for in the commit, not a way to silence the floor.",
 "starved": {}
}
