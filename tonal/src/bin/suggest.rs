//! Module 12 CLI — placeholder while the port lands; the suggester follows
//! in its own commit.
fn main() {
    eprintln!("suggest: module 12 lands in the next commit");
    std::process::exit(2);
}
